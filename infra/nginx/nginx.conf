server {
    listen 80;
    server_name api.kyberncloud.com; 

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Example SSL Block (to be enabled after running certbot)
# server {
#     listen 443 ssl;
#     server_name _; # Replace with your domain
#
#     ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem; # Standard Certbot path
#     ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem; # Standard Certbot path
#
#     location / {
#         proxy_pass http://localhost:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
